class LottieAsset {
  /// Animated ChessEver brand mark (4 cyan squares + king) used on the
  /// auth/login screen. Transparent background; renders over BlurBackground.
  static const chesseverLogo = 'assets/lottie/chessever_logo.json';
}
